<Page
    x:Class="Project_D.Views.StorageMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_D.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Project_D.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="FileTemplate" x:DataType="viewmodels:StorageViewModel">
            <StackPanel Orientation="Horizontal">
                <Image Name="Picture" Source="{x:Bind Thumbnail}"></Image>
                <StackPanel>
                    <TextBlock Text="{x:Bind DisplayName,Mode=TwoWay}"></TextBlock>
                    <TextBlock Text="{x:Bind Path}"></TextBlock>
                </StackPanel>

            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton Icon="Accept" Label="P"></AppBarButton>
            <AppBarButton Icon="Cancel" Label="P"></AppBarButton>
            <AppBarButton Icon="Save" Label="S"></AppBarButton>
            <CommandBar.Content>
                <RelativePanel VerticalAlignment="Stretch" >
                    <TextBlock Text="Select" VerticalAlignment="Center"></TextBlock>
                    <ComboBox VerticalAlignment="Center" MinHeight="48" x:Name="list" RelativePanel.AlignRightWithPanel="True">
                        <ComboBoxItem>1</ComboBoxItem>
                        <ComboBoxItem>2</ComboBoxItem>
                        <ComboBoxItem>3</ComboBoxItem>
                        <ComboBoxItem>4</ComboBoxItem>
                        <ComboBoxItem>5</ComboBoxItem>
                    </ComboBox>
                    
                </RelativePanel>
            </CommandBar.Content>
        </CommandBar>
    </Page.TopAppBar>

    <Grid Name="FileMainGrid" AllowDrop="True" 
          Drop="FileMainGrid_Drop"
          DragOver="FileMainGrid_DragOver">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <GridView Grid.Row="0" ItemsSource="{x:Bind StorageMain.Storages}" 
                  ItemTemplate="{StaticResource FileTemplate}">
        </GridView>

    </Grid>
</Page>
