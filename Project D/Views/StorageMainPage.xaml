<Page
    x:Class="Project_D.Views.StorageMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_D.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Project_D.ViewModels"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="FileTemplate" x:DataType="viewmodels:StorageViewModel">
            <StackPanel Orientation="Horizontal">
                <Image Name="Picture" Source="{x:Bind Thumbnail}"></Image>
                <StackPanel>
                    <TextBlock Text="{x:Bind DisplayName,Mode=TwoWay}"></TextBlock>
                    <TextBlock Text="{x:Bind Path}"></TextBlock>
                </StackPanel>

            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Name="FileMainGrid" AllowDrop="True" 
          Drop="FileMainGrid_Drop"
          DragOver="FileMainGrid_DragOver">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <CommandBar  Grid.Column="1" >
                <CommandBar.Content>
                    <AppBarButton Style="{StaticResource BackAppBarButton}"></AppBarButton>
                </CommandBar.Content>
                <AppBarButton Icon="Accept" Label="P"></AppBarButton>
                <AppBarButton Icon="Cancel" Label="P"></AppBarButton>
                <AppBarButton Icon="Save" Label="S"></AppBarButton>

            </CommandBar>
            <AutoSuggestBox Grid.Column="2" Width="200"></AutoSuggestBox>
        </Grid>

        <controls:DataGrid Grid.Row="1" x:Name="dataGrid1" ItemsSource="{x:Bind StorageMain.Storages}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn 
                    Header="DisplayName" 
                    Width="SizeToHeader"
                    Binding="{x:Bind viewmodels:StorageViewModel.DisplayName}" 
                    FontSize="20" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <GridView Grid.Row="2" ItemsSource="{x:Bind StorageMain.Storages}" 
                  ItemTemplate="{StaticResource FileTemplate}">
        </GridView>

    </Grid>
</Page>
